# Quick Start: Оптимизированная синхронизация Supabase

## Для новых пользователей

### Шаг 1: Создайте проект в Supabase
1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект (бесплатный tier)
3. Дождитесь создания БД

### Шаг 2: Создайте таблицы
1. Откройте `SQL Editor` в Supabase Dashboard
2. В приложении YourOwnAI откройте **Настройки → Облачная синхронизация**
3. Нажмите кнопку **"Показать SQL схему"**
4. Скопируйте SQL код
5. Вставьте в SQL Editor в Supabase и выполните

Или используйте файл `supabase_schema.sql` из репозитория.

### Шаг 3: Подключите приложение
1. В Supabase Dashboard → `Settings` → `API`
2. Скопируйте:
   - **Project URL**
   - **anon/public key**
3. В приложении введите эти данные в настройках
4. Нажмите **"Проверить подключение"**

### Шаг 4: Синхронизируйте данные
1. Нажмите **"Загрузить на облако"**
2. Дождитесь завершения
3. Готово! ✅

---

## Для существующих пользователей (миграция)

Если вы уже используете старую схему синхронизации:

### Вариант А: Создать новый проект (рекомендуется)
1. Создайте новый Supabase проект
2. Следуйте инструкциям выше
3. Загрузите данные заново

### Вариант Б: Обновить существующий проект
1. В Supabase SQL Editor выполните команду удаления старых таблиц:
```sql
DROP TABLE IF EXISTS document_embeddings CASCADE;
DROP TABLE IF EXISTS knowledge_documents CASCADE;
DROP TABLE IF EXISTS system_prompts CASCADE;
```

2. Выполните новую схему (см. Шаг 2 выше)

3. В приложении сделайте **"Загрузить на облако"** заново

⚠️ **Внимание:** Старые данные (RAG документы, embeddings) будут потеряны в облаке, но останутся локально.

---

## Что синхронизируется

### ✅ Синхронизируется:
- 📝 Диалоги (conversations)
- 💬 Сообщения (messages) + вложения
- 🧠 Воспоминания (memories) - только текст
- 👤 Персоны (personas) - полностью

### ❌ НЕ синхронизируется (хранится локально):
- 📄 Системные промпты
- 📚 RAG документы
- 🔢 Эмбеддинги (генерируются локально)

---

## FAQ

**Q: Почему не синхронизируются эмбеддинги?**  
A: Они занимают ~70% места. Генерируются быстро локально, не нужно хранить в облаке.

**Q: Что делать, если на новом устройстве нет системных промптов?**  
A: При первом запуске приложение автоматически создаёт дефолтные промпты.

**Q: Как перенести RAG документы между устройствами?**  
A: Используйте экспорт/импорт через файл (функция в разработке).

**Q: Сколько места займут мои данные?**  
A: Примерно:
- 100 диалогов = 50 KB
- 1000 сообщений = 500 KB  
- 500 воспоминаний = 100 KB
- 10 персон = 40 KB
- **Итого:** ~700 KB

Free tier Supabase: 500 MB → хватит на >700 таких наборов данных!

**Q: Что если превышу лимит?**  
A: Supabase не блокирует сразу. Можно:
1. Удалить старые диалоги
2. Архивировать данные локально
3. Перейти на платный план ($25/месяц за 8 GB)

---

## Мониторинг

### Проверить размер БД:
1. Supabase Dashboard → Database → размер проекта
2. В приложении → Настройки → Облачная синхронизация → "Проверить подключение"

### Оптимизация:
- Архивируйте старые диалоги (не синхронизируются)
- Удаляйте ненужные воспоминания
- Ограничьте количество персон (если много)

---

## Поддержка

Если что-то пошло не так:
1. Проверьте логи в приложении
2. Проверьте Supabase Dashboard → Logs
3. Создайте issue на GitHub

---

Версия: 1.0 (оптимизированная схема)  
Дата: 2026-02-14
