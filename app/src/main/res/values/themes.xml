<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme (will be overridden by Compose) -->
    <!-- Changed to Theme.MaterialComponents for AppCompat support -->
    <style name="Theme.YourOwnAI" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
